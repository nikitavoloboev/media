# Media

Used for images, assets for various GitHub repos. Folder stands for the repo name assets are used in and referenced to the raw asset. Inside everything is sorted by date so links never die out if you view assets in old commits in git history.
